@using System.Collections.Generic
@using System.Data
@model Learning.Models.AuthorAllModel

﻿@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Search Result";
}

<!DOCTYPE html>

<!--search box-->
<div id="author-head-content">
    <div id="custom-search-input">
        <div class="input-group col-md-12" style="margin-top: 50px;">
            @using (Html.BeginForm("Search", "Home", FormMethod.Post))
            {
                <div class="input-group" style="margin-bottom:10px; width:100%;">
                    @Html.TextBoxFor(m => m.searchAuthor.key, new { @class = "form-control" })
                    <div class="input-group-btn">
                        <input type="submit" class="btn btn-primary" value="Search" />
                    </div>
                </div>
            }
        </div>
    </div>
</div>
<!--result box-->
<div id="author-result-content">
    <table class="table author-table" style="margin-top: 20px;">
        <tbody id="author_tab_body">
            @if (Model.searchAuthor.authorList.Rows.Count > 0)
            {
                for (int i = 0; i < Model.searchAuthor.authorList.Rows.Count; i++)
                {
                    <tr class="tab_body_r">
                        <td class="tab_body_a">
                            @Model.searchAuthor.authorList.Rows[i]["ID"].ToString()
                        </td>
                        <td class="tab_body_a">
                            @Model.searchAuthor.authorList.Rows[i]["Nama"].ToString()
                        </td>
                        <td>@Html.ActionLink("View", "AuthorProfile", new { id = Model.searchAuthor.authorList.Rows[i]["ID"].ToString() })</td>
                        @if (Session["LogedUserID"] != null && Model.searchAuthor.authorList.Rows[i]["ID Account"].ToString().Equals("0"))
                        {
                            <td>@Html.ActionLink("Merge", "MergeAction", new { id = Model.searchAuthor.authorList.Rows[i]["ID"].ToString() })</td>
                        }
                    </tr>
                }
            }
            else
            {
                <tr class="tab_body_r">
                    <td class="tab_body_a">
                        <span>Hasil tidak ditemukan</span>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    @*<div class="author-table-foot">
            <div class="result-info-text">
                <p>Menampilkan 2 dari xxx.xxx hasil</p>
            </div>
            <div class="result-info-btn">
                <button class="btn result-btn">1</button>
            </div>
        </div>*@
</div>
